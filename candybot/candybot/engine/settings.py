class Settings:
    def __init__(self, prefix, chance, pick_limit, min_, max_, cap):
        self.prefix = prefix
        self.chance = chance
        self.pick_limit = pick_limit
        self.min = min_
        self.max = max_
        self.cap = cap
